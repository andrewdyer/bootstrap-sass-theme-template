{
  "name": "bootstrap-sass-theme-template",
  "version": "0.1.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/andrewdyer/bootstrap-sass-theme-template.git"
  },
  "homepage": "https://github.com/andrewdyer/bootstrap-sass-theme-template",
  "main": "dist/css/styles.css",
  "style": "dist/css/styles.css",
  "sass": "src/scss/styles.scss",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build:sass": "sass src/scss/styles.scss dist/css/styles.css --no-source-map",
    "build:prefix": "postcss dist/css/styles.css --use autoprefixer -o dist/css/styles.css",
    "build:min": "sass src/scss/styles.scss dist/css/styles.min.css --style=compressed --no-source-map && postcss dist/css/styles.min.css --use autoprefixer -o dist/css/styles.min.css",
    "build": "npm-run-all clean build:sass build:prefix",
    "watch:sass": "sass --watch src/scss/styles.scss dist/css/styles.css --no-source-map",
    "watch:prefix": "onchange 'dist/css/styles.css' -- postcss dist/css/styles.css --use autoprefixer -o dist/css/styles.css",
    "dev": "npm-run-all --parallel watch:sass watch:prefix",
    "format": "prettier --write '**/*.{scss,css,js,json,md}'",
    "lint": "stylelint '**/*.scss'",
    "prepare": "husky install",
    "prepublishOnly": "npm-run-all build build:min"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "npm-run-all": "^4.1.5",
    "onchange": "^7.1.0",
    "postcss": "^8.5.6",
    "postcss-cli": "^11.0.1",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "sass": "^1.93.2",
    "stylelint": "^16.25.0",
    "stylelint-config-clean-order": "^7.0.0",
    "stylelint-config-standard-scss": "^16.0.0",
    "stylelint-scss": "^6.12.1"
  },
  "lint-staged": {
    "*.scss": [
      "prettier --write",
      "stylelint --fix"
    ]
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ],
  "engines": {
    "node": ">=22.12.0"
  }
}
